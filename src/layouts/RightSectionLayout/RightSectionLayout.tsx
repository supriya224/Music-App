import React from 'react';

function RightSectionLayout({ children }: { children: React.ReactNode }) {
  return <section id="right-section">{children}</section>;
}

export default RightSectionLayout;
